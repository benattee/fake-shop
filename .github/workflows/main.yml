name: CI-CD
on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  CI:
    runs-on: ubuntu-latest
    steps:
      -  run: echo "Obter código"
      -  run: echo "Executar o Docker_build"
      -  run: echo "Enviar a imagem docker pro DokerHub"
  CD:
    needs: [CI]
    runs-on: ubuntu-latest
    steps:
      -  run: echo "Obter código"
      -  run: echo "Configurar o Kubeconfig"
      -  run: echo "Executar o Apply"
  
